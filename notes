- insert label should only show outside of menu

- multiline:
    * fix endless insert tags after `dd` and `ESC``i`
    * fix visual_goto_up/down not working

- fix sebi bugs

- add to escape
    [x] `^`
    [x] `$` 
    [x] `a` 
    [ ] `s` 
    [x] `0`
    [x] `C`
    [x] `c`

- add to visual
    [x] `^`
    [x] `0`
    [x] `c`

- add goto_line_up & goto_line_down
